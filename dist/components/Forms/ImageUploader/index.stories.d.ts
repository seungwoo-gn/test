import React from "react";
declare const _default: {
    title: string;
    component: ({ width, height, handleUpload, body, selectedImage, setSelectedImage, handleCancel, isProgress, progressPercent, }: import("./types").ImageUploaderProps) => React.JSX.Element;
    parameters: {
        componentSubtitle: string;
    };
};
export default _default;
export declare const DefaultImageUploader: () => React.JSX.Element;
